{
  "name": "bison-sdk-docs",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Documentation for bison-jib-sdk",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "bun test && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0"
  },
  "dependencies": {
    "bison-jib-sdk": "1.0.0",
    "gsap": "^3.15.0"
  }
}
